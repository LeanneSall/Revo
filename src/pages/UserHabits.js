import { CircularProgress } from "@material-ui/core";
import React, { Suspense } from "react";
import Tracking from "../components/Habits/tracking";

export default function UserHabits() {
  return (
    <div>
      <Tracking />
    </div>
  );
}
