import React, { useEffect } from "react";
import {
  Button,
  CircularProgress,
  Container,
  Paper,
  Typography,
} from "@material-ui/core";
import TrackBar from "./trackBar";

export default function TrackSpecific({ i }) {
  console.log(i);
  return (
    <div>
      <h1>hello</h1>
    </div>
  );
}

//<Typography>props</Typography>
